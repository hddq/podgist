# Required for gPodder sync
GPODDER_USERNAME=your_username
GPODDER_PASSWORD=your_password

# Optional: set when your LLM backend requires auth
LLM_API_KEY=your_llm_api_key

# Optional: set when your Whisper backend requires auth
WHISPER_API_KEY=optional_whisper_api_key
